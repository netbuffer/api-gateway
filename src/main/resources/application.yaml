spring:
  profiles:
    active: dev
  application:
    name: @project.name@
  output:
    ansi:
      enabled: always
endpoints:
  shutdown:
    enabled: true
# 环境变量
---
spring:
  profiles: dev
server:
  context-path: /dev
  port: 8081
#  打印调试信息
debug: true
logging:
  config: classpath:logback-springboot.xml
---
spring:
  profiles: test
server:
  context-path: /test
  port: 8082
---
spring:
  profiles: product
server:
  context-path: /product
  port: 80